import SwiftUI

struct AccountSceneSidebar: View {
    var body: some View {
        VStack(alignment: .leading) {
            Spacer()
            
            AccountFeature_SignOutBtn()
        }
        .padding(12)
    }
}

fileprivate struct ProfileCard: View {
    var body: some View {
        VStack {
            
        }
    }
}

