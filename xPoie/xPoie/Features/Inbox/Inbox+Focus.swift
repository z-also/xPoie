import SwiftUI

struct InboxFocus: View {
    var body: some View {
        VStack {
            FocusFeatureSetter()
        }
//        .glassEffect(.regular, in: RoundedRectangle(cornerRadius: 20))
    }
}

