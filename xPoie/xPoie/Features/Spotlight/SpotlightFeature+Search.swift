import SwiftUI

struct SpotlightSearch: View {
    var body: some View {
        VStack(alignment: .leading, spacing: 0) {
            Text("search")
        }
        .frame(maxWidth: .infinity)
    }
}
